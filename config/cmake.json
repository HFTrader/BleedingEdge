[ {
    "version":   "3.2.2",
    "tags": ["latest"],
    "url":       "http://www.cmake.org/files/v3.2/{dirname}.tar.gz",
    "dirname":   "{name}-{version}",
    "depends":   [ "gcc" ],
    "configure": "CXX={deploydir}/bin/g++ CC={deploydir}/bin/gcc ./configure --prefix={installdir}/{dirname}"
},
 {
    "version":   "2.8.2",
    "tags": ["default","stable"],
    "url":       "http://www.cmake.org/files/v3.2/{dirname}.tar.gz",
    "dirname":   "{name}-{version}",
    "depends":   [ "gcc" ],
    "configure": "CXX={deploydir}/bin/g++ CC={deploydir}/bin/gcc ./configure --prefix={installdir}/{dirname}"
} ]
