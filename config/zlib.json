[ {
    "version":   "1.2.8",
    "tags": ["latest","stable","default"],
    "url":       "http://zlib.net/{name}-{version}.tar.gz",
    "dirname":   "{name}-{version}",
    "configure": "./configure --prefix={installdir}/{dirname}"
} ]
