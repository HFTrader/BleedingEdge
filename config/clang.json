[ {
    "version":   "svn",
    "depends": [ "cmake" ],
    "url":   "http://llvm.org/releases/{version}",
    "dirname":   "{name}-{version}",
    "configure": "mkdir build; cd build; cmake -G \"Unix Makefiles\" -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX={installdir}/{dirname} -DLLVM_ENABLE_ASSERTIONS=No -DLLVM_TARGETS_TO_BUILD=X86 -DLLVM_ENABLE_CXX1Y=ON ..",
    "make": "cd {builddir}/clang-{version}/build; make -j",
    "install": "cd {builddir}/clang-{version}/build; make install"
},
{   "version":   "3.7.1",
    "depends": [ "cmake", "gcc-5.3.0" ],
    "url":   "http://llvm.org/releases/{version}",
    "dirname":   "{name}-{version}",
    "configure": "mkdir build; cd build; cmake -G \"Unix Makefiles\" -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX={installdir}/{dirname} -DLLVM_ENABLE_ASSERTIONS=No -DLLVM_TARGETS_TO_BUILD=X86 -DLLVM_ENABLE_CXX1Y=ON ..",
    "make": "cd {builddir}/clang-{version}/build; make -j",
    "install": "cd {builddir}/clang-{version}/build; make install"
},
{   "version":   "3.5.2",
    "depends": [ "cmake" ],
    "url":   "http://llvm.org/releases/{version}",
    "dirname":   "{name}-{version}",
    "configure": "mkdir build; cd build; cmake -G \"Unix Makefiles\" -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX={installdir}/{dirname} -DLLVM_ENABLE_ASSERTIONS=No -DLLVM_TARGETS_TO_BUILD=X86 -DLLVM_ENABLE_CXX1Y=ON ..",
    "make": "cd {builddir}/clang-{version}/build; make -j",
    "install": "cd {builddir}/clang-{version}/build; make install"
} ]
